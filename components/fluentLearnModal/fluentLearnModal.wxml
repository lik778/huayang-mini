<view class="fluent-learn-modal" wx:if="{{didVisible}}">
    <view class="box-wrap {{show?'box-fade-in':'box-fade-out'}}">
        <view class="modal-message">{{message}}</view>
        <view class="btn" bindtap="confirm">{{confirmText}}</view>
    </view>
</view>
