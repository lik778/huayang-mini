<view class="action-nav-bar" style="padding-top:{{statusBarHeight}}px;background:{{color === '' ? '#fff' : color}};">
    <view class="back-module" bind:tap="checkoutMute">
        <view class="back-icon" style="display: {{hideIcon ? 'none': ''}};">
            <image src="{{didMute ? '../../assets/images/common/mute.png' : '../../assets/images/common/music.png'}}"></image>
        </view>
    </view>
    <view class="title" style="color:{{titleColor}}">{{title}}</view>
</view>
