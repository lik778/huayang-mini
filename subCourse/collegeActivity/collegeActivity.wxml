<view class="activitybox">
    <view class="activity" wx:key="index" wx:for="{{activityList}}">
        <image class="activity-img" src="{{item.cover_url}}" />
        <view class="activity-title">
            {{item.title}}
        </view>
    </view>
</view>
